{
  "name": "agentic-ai-rag-mcp-chatbot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Full-stack agentic AI chatbot with RAG, MCP tool-calling, Angular + Node.js (Coursera course scaffold)",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend -- --configuration=production",
    "build:vercel": "npm run build:backend && npm run build:frontend"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^2.23.0",
    "cors": "^2.8.6",
    "dotenv": "^16.4.7",
    "express": "^4.22.3",
    "openai": "^7.20.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.25",
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
